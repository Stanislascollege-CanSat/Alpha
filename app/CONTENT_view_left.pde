import java.lang.Runnable;

public Runnable CONTENT_view_left = new Runnable(){
  public void run(){
    // ===== VIEW CONTENTS HERE (SIMILAR TO draw() LOOP) ===== //

    stroke(0);
    fill(0);
    textAlign(CENTER);
    textSize(20);
    text("Left View",0,0);


  }
};
